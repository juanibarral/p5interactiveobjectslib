package util;

/**
 * It is the interface for listening events launched by pickeable objects
 * @author Juan Camilo Ibarra
 * @version 0.5b
 */
public interface PickeableObjectListener {
	public void eventTriggered(PickeableObjectEvent event);
}
